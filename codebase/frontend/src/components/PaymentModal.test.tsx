describe('PaymentModal', () => {
  it('should render the payment modal successfully', () => {
    //TODO implement
  });
  
  it('should run the onSubmit function successfully and run the makePayment mutation', () => {
    //TODO implement
  });

  it('should show an error modal when the onSubmit makePayment mutation fails', () => {
    //TODO implement
  });

  it('should show a loading state when the makePayment mutation is loading', () => {
    //TODO implement
  });
});